var options =
            {
                animation : true,
            };
            
            var element = document.getElementById( 'EpicButton' );
            
            var tooltip = new bootstrap.Tooltip( element, options );